services:
  - type: web
    name: kaapav-n8n-bot
    env: docker
    plan: free
    region: singapore
    autoDeploy: true

    disk:
      name: n8n_data
      mountPath: /home/node/.n8n
      sizeGB: 1
